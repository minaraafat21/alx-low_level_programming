#include "main.h"
/**
 * _abs - checks for absolute value
 *
 * @r: parameter
 *
 * Return: returns same number if positive else negative
 *
 */

int _abs(int r)
{
	if (r >= 0)
		return (r);
	return (-r);
}
